# SDK C++ experimental

Wrapper mínimo C++17 sobre a ABI C: `std::optional<nm_fraction>` evita sentinelas inválidas. O binário deve vincular a implementação `sdk/c/nabla_fraction.c`. Consulte a especificação v0 em `protocol/schema/README.md`. Sem avaliação de expressões ou prova Lean neste SDK.
